var Logar = document.querySelector("#entrar"); // botão que utiliza a variavel para salvar
Logar.addEventListener("click", function () { // Faz a leitura do botão apartir de um click do usuario
  event.preventDefault();
  var form = document.querySelector("#form-login"); // Seleciona o formulário que possui os dados

  var logar = (form); // Obtem o paciente pelo Formulário
})